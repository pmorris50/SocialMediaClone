const {ObjectId} = require('mongoose').Types;
const {Thought, User} = require('../models');

//Aggregate function to count the number of Thoughts


//Aggregate function to count number of reactions for each thought use $match

module.exports = {
    
}